# Django Settings
SECRET_KEY=your-secret-key-here
DEBUG=False
ALLOWED_HOSTS=your-render-app.onrender.com,localhost

# CORS - Add your frontend URLs (comma-separated)
CORS_ALLOWED_ORIGINS=http://localhost:3000,https://your-frontend-domain.com

# Database (Render will provide this automatically)
# DATABASE_URL=postgres://user:password@host:port/database

# Firebase Admin SDK
# Add your Firebase credentials path or use environment variable
# GOOGLE_APPLICATION_CREDENTIALS=/path/to/firebase-credentials.json

# Google Gemini AI (for chatbot)
# Get your API key from: https://makersuite.google.com/app/apikey
GEMINI_API_KEY=
